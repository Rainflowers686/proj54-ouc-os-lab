# 全仓库 Markdown 重写大纲

## 目标

本文件定义全仓库 Markdown 重写后的文档分层和质量门控。

## 适用对象

适用于文档维护者和审计人员。

## 内容范围

文档分为首页入口、正式提交材料、课程实验材料、patch 复现材料、证据与提交材料、展示材料、测试记录、外部依赖说明、任务计划和历史记录。

## 结构规范

正式说明文档使用目标、适用对象、内容范围、结构规范、语言风格、质量标准、边界条件。任务书保留任务结构、提交要求和评分标准。索引文档保留生成说明。

## 语言风格

项目文档使用严谨中文技术写作，保留必要命令和表格。

## 质量标准

所有 current final 事实应统一指向 `db85947 / integrated 0001-0009`。

## 边界条件

不提交 `external/xv6-riscv/`、raw logs、summary 原件、视频、截图、token、密码或隐私材料。不把 timeout evidence 写成长期稳定性测试。不把 `pgcount`/`memstat` 写成完整内存管理；不把 `fcount`/`fdcount`/`fdinfo` 写成完整文件系统；Lab5 不新增内核机制。
